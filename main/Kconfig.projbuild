menu "CAN Bridge Configuration"
    depends on SOC_TWAI_SUPPORTED

    orsource "$IDF_PATH/examples/common_components/env_caps/$IDF_TARGET/Kconfig.env_caps"

    config CAN_TX_GPIO
        int "CAN TX GPIO"
        default 4
        help
            GPIO pin for CAN TX signal.

    config CAN_RX_GPIO
        int "CAN RX GPIO"
        default 5
        help
            GPIO pin for CAN RX signal.

endmenu
